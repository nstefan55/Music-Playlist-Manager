How can I modify this code for example if I create playlist "Popis" inside my app.rb, the main GUI window, it saves that "Popis" playlist with this data structure from the spremiPlaylistu method: 
```
else
                playlistData = {
                    naziv: playlist.naziv,
                    pjesme: []
                }
                existingPlaylists << playlistData
            end
```
And for example I want to add a song to that playlist "Popis", the logic of the code for that is inside songVIew.rb file, more precisely this code: 
```
addSongButton = FXButton.new(verticalFrame, "Dodaj pjesmu u playlistu")
    addSongButton.connect(SEL_COMMAND) do

        playlistNameDialog = FXInputDialog.new(self, "Dodaj pjesmu u playlistu", "Unesite naziv playliste:")
        playlistNameDialog.execute
        nazivPlayliste = playlistNameDialog.text.strip


        songNameDialog = FXInputDialog.new(self, "Dodaj pjesmu u playlistu", "Unesite naziv pjesme:")
        songNameDialog.execute
        nazivPjesme = songNameDialog.text.strip


        artistNameDialog = FXInputDialog.new(self, "Dodaj pjesmu u playlistu", "Unesite ime izvođača:")
        artistNameDialog.execute
        izvodac = artistNameDialog.text.strip

        albumNameDialog = FXInputDialog.new(self, "Dodaj pjesmu u playlistu", "Unesite naziv Albuma:")
        albumNameDialog.execute
        album = albumNameDialog.text.strip


        lengthDialog = FXInputDialog.new(self, "Dodaj pjesmu u playlistu", "Unesite duljinu trajanja pjesme (u minutama): ")
        lengthDialog.execute
        trajanje = lengthDialog.text.strip.to_i

        if !nazivPlayliste.empty? && !nazivPjesme.empty? && !izvodac.empty? && !album.empty? && trajanje > 0
            begin
                playlist = @playlists.find { |p| p.naziv == nazivPlayliste }
                if playlist
                    song = Song.new(nazivPjesme, izvodac, album, trajanje)
                    playlist.dodajPjesmu(song)
                    spremiPlaylistu(playlist)
                    FXMessageBox.information(self, MBOX_OK, "Success", "Pjesma  '#{nazivPjesme}'  uspješno dodana u playlistu  '#{nazivPlayliste}' !")
                else
                    FXMessageBox.error(self, MBOX_OK, "Error", "Playlista pod nazivom:  '#{nazivPlayliste}'  ne postoji!")
                end
            rescue ArgumentError => e
                FXMessageBox.error(self, MBOX_OK, "Error", e.message)
            end
        else
            FXMessageBox.error(self, MBOX_OK, "Error", "Molim vas unesite sve tražene parametre!")
        end
    end
```
I want the app to be able to update the json file to this particular sturcture in order to contain all data that is typed by the user for adding a song: 
```
if existing_playlist
                playlistData = {
                    naziv: playlist.naziv,
                    pjesme: playlist.pjesme.map do |pjesma|
                        {
                            naziv: pjesma.naziv,
                            izvodac: pjesma.izvodac,
                            album: pjesma.album,
                            trajanje: pjesma.trajanje
                        }
                    end
                }
```
The Full code of the method I want you to modify is the spremiPlaylstu method: 
```
def spremiPlaylistu(playlist)
        file_path = 'data/playlists.json'

        # Read existing playlists from the file
        existingPlaylists = []
        if File.exist?(file_path) && !File.zero?(file_path)
            jsonData = File.read(file_path)
            existingPlaylists = JSON.parse(jsonData)
        end

        begin
            # Check if the playlist already exists
            existing_playlist = existingPlaylists.find { |p| p['naziv'] == playlist.naziv }
            if existing_playlist
                playlistData = {
                    naziv: playlist.naziv,
                    pjesme: playlist.pjesme.map do |pjesma|
                        {
                            naziv: pjesma.naziv,
                            izvodac: pjesma.izvodac,
                            album: pjesma.album,
                            trajanje: pjesma.trajanje
                        }
                    end
                }
            else
                playlistData = {
                    naziv: playlist.naziv,
                    pjesme: []
                }
                existingPlaylists << playlistData
            end

            jsonData = JSON.generate(existingPlaylists)
            File.write(file_path, jsonData)
            puts "Playlista pod nazivom:  '#{playlist.naziv}'  je uspješno spremljena u datoteku."
        rescue StandardError => e
            puts "Greška:  #{e.message}"
            FXMessageBox.error(self, MBOX_OK, "Error", "Pojavila se greška prilikom spremanja playliste!")
        end
    end
```